<Window x:Class="DockerDashboard.Views.BranchSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="切換 Git 分支" Height="480" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 標題 -->
        <TextBlock Grid.Row="0" Text="{Binding ProjectName}" 
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Margin="0,0,0,12" />

        <!-- 搜尋框 -->
        <TextBox Grid.Row="1" x:Name="SearchBox"
                 materialDesign:HintAssist.Hint="搜尋分支..."
                 materialDesign:TextFieldAssist.HasClearButton="True"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,12"
                 TextChanged="SearchBox_TextChanged" />

        <!-- 分支清單 -->
        <ListBox Grid.Row="2" x:Name="BranchListBox"
                 SelectionMode="Single"
                 MouseDoubleClick="BranchListBox_DoubleClick"
                 VirtualizingStackPanel.IsVirtualizing="True">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="4,2">
                        <materialDesign:PackIcon Kind="{Binding Icon}" 
                                                 Foreground="{Binding Color}"
                                                 VerticalAlignment="Center" Width="18" Height="18" />
                        <TextBlock Text="{Binding Name}" Margin="8,0,0,0" 
                                   VerticalAlignment="Center"
                                   FontWeight="{Binding FontWeight}" />
                        <TextBlock Text="(目前)" Margin="6,0,0,0" 
                                   Foreground="#4CAF50" FontSize="11"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding IsCurrentVisibility}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- 按鈕列 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="取消" 
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="Cancel_Click" Margin="0,0,8,0" />
            <Button Content="切換分支" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="Confirm_Click"
                    IsEnabled="{Binding HasSelection}" />
        </StackPanel>
    </Grid>
</Window>
